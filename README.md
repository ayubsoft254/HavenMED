# HavenMED
``` A comprehensive healthcare management system designed to streamline patient care, healthcare professional collaboration, and institutional operations. ```

# Features:

## Quick Start (Local)

Run from the `src` directory:

```bash
python manage.py migrate
python manage.py populate_test_data
python manage.py runserver
```

Open:
- `http://127.0.0.1:8000/`
- Login: `http://127.0.0.1:8000/accounts/login/`

## How To Use HavenMED By Role

### Doctors / Healthcare Professionals
1. Sign in using a healthcare professional account.
2. Open dashboard: `/accounts/dashboard/healthcare/`.
3. Review upcoming and pending appointments.
4. For virtual appointments, use the appointment details page and join consultation when available.
5. Keep profile and professional details updated from profile edit.

### Clinics and Hospitals (Institutions)
1. Sign in with a clinic or hospital account.
2. Open dashboard: `/accounts/dashboard/institution/`.
3. Review institutional overview metrics and activity.
4. Use dashboard search and profile tools to manage institution presence.

### Patients
1. Sign in with a patient account.
2. Open provider directory: `/services/providers/`.
3. Select a provider and book appointment (virtual, in-person, or home visit based on availability).
4. Complete payment where required and track status.
5. Manage appointments from dashboard and appointment detail pages.

## Test Accounts

Seed accounts are generated by:

```bash
python manage.py populate_test_data
```

Shared password for all test accounts:

```text
TestPass123!
```

### Admin
- `admin@havenmed.co.ke`

### Healthcare Professionals (Doctors)
- `james.kariuki@test.havenmed.co.ke`
- `fatuma.ali@test.havenmed.co.ke`
- `kevin.mutua@test.havenmed.co.ke`
- `grace.ochieng@test.havenmed.co.ke`
- `samuel.njoroge@test.havenmed.co.ke`
- `lilian.chebet@test.havenmed.co.ke`

### Clinics / Hospital
- `afya.clinic@test.havenmed.co.ke` (Clinic)
- `health.point@test.havenmed.co.ke` (Clinic)
- `nairobi.health@test.havenmed.co.ke` (Hospital)

### Patients
- `amina.hassan@test.havenmed.co.ke`
- `brian.omondi@test.havenmed.co.ke`
- `cynthia.wanjiku@test.havenmed.co.ke`
- `david.kipchoge@test.havenmed.co.ke`
- `esther.mwangi@test.havenmed.co.ke`

## Production Docker Deployment (Linux + External Nginx)

This repository now includes production deployment files designed for:
- Linux server hosting
- Dockerized Django/Gunicorn app
- External host-level Nginx (not containerized)
- Coexistence with other Nginx sites/vhosts on the same server

### Production files
- `docker-compose.prod.yml`: app-only production compose stack
- `Makefile`: operational commands for build/run/migrations/logs
- `nginx/havenmed.external.conf`: host Nginx vhost template

### 1. Prepare environment

```bash
cp .env.example .env
```

Update `.env` values, especially:
- `SECRET_KEY`
- `ALLOWED_HOSTS`
- `CSRF_TRUSTED_ORIGINS`
- `APP_PORT`
- `HOST_DATA_DIR`, `HOST_MEDIA_DIR`, `HOST_STATIC_DIR`

### 2. Create persistent directories

```bash
make init
```

### 3. Build and start app

```bash
make build
make up
```

The app listens on `127.0.0.1:${APP_PORT}` (default `127.0.0.1:18000`) so it is only reachable through Nginx.

### 4. Configure external Nginx

Copy `nginx/havenmed.external.conf` to your server's Nginx sites directory and update:
- `server_name`
- optional TLS config

Then enable and test:

```bash
sudo nginx -t
sudo systemctl reload nginx
```

This vhost does not use `default_server`, so it can run alongside other existing Nginx configs.

### 5. Useful operations

```bash
make logs
make ps
make migrate
make collectstatic
make createsuperuser
make restart
make down
```